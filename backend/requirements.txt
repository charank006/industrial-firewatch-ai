fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# Persistence (Phase 0b / 2)
sqlalchemy>=2.0.28
# SQLAlchemy's async bridge needs greenlet; it is not always pulled in by the
# base install on Python 3.12 and its absence only surfaces at first query.
greenlet>=3.0.3
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1
geoalchemy2>=0.14.7

# Geometry (Phase 4 - OSM area computation)
shapely>=2.0.3

# Config & schemas
pydantic>=2.6.4
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# External APIs: NASA FIRMS, Open-Meteo, Overpass
httpx>=0.27.0

# Feature engineering (Phase 5) and ML (Phase 8)
numpy>=1.26.4
pandas>=2.2.1

# Scheduling (Phase 4 minimal worker / Phase 7)
apscheduler>=3.10.4

# Tests. respx keeps tests off NASA/Overpass; freezegun makes the
# same-local-hour weather baseline logic testable at all.
pytest>=8.1.1
pytest-asyncio>=0.23.6
respx>=0.21.0
freezegun>=1.4.0
